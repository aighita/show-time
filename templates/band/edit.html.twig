{% extends 'base.html.twig' %}

{% block title %}Edit Band{% endblock %}

{% block body %}
    <div class="flex min-h-screen bg-background text-foreground">
        <!-- Sidebar via React -->
        <div {{ react_component('AppSidebar') }}></div>

        <!-- Main Content -->
        <main class="flex-1 p-8 space-y-8">
            <!-- Header -->
            <div class="flex justify-between items-center">
                <h1 class="text-3xl font-bold tracking-tight">Edit Band</h1>
                <a href="{{ path('app_band_index') }}" class="btn btn-ghost btn-sm">
                    ← Back to list
                </a>
            </div>

            <!-- Form Card -->
            <div class=" w-full bg-card text-card-foreground rounded-2xl p-10 space-y-6">
                {{ include('band/_form.html.twig', {
                    button_label: 'Update',
                    showDelete: true
                }) }}

                {#                <div class="pt-4"> #}
                {#                {{ include('band/_delete_form.html.twig') }} #}
                {#                </div> #}

                {#                <div {{ react_component('DeleteBandDialog', { #}
                {#                    bandId: band.id, #}
                {#                    csrfToken: csrf_token('delete' ~ band.id), #}
                {#                }) }}></div> #}

            </div>
        </main>
    </div>
{% endblock %}
